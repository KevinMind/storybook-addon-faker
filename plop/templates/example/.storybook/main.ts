import { StorybookConfig } from "@storybook/react/types";

import createConfig from "../../.storybook/main";

// add custom storybook config
const config: StorybookConfig = createConfig({});

export default config;
