{
  "name": "seed-story",
  "version": "0.0.6",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "license": "MIT",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "test": "jest",
    "build": "tsup src/index.tsx --format cjs --dts --external @faker-js/faker",
    "dev": "yarn build --watch",
    "lint": "TIMING=1 eslint src",
    "format": "yarn lint --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@storybook/react": "^6.5.12",
    "@types/jest": "^26.0.22",
    "chromatic": "^6.10.1",
    "eslint": "^7.32.0",
    "jest": "^26.6.3",
    "scripts": "*",
    "tsconfig": "*",
    "tsup": "^5.10.1",
    "typescript": "^4.5.3"
  },
  "jest": {
    "preset": "scripts/jest/node"
  },
  "peerDependencies": {
    "@faker-js/faker": "^7.5.0",
    "chromatic": "^6.10.1"
  }
}
